import React, { Component } from "react";
import { Accordion, Icon, Grid, Header, Image } from "semantic-ui-react";

const AllergensAliases = ({ props }) => {
  return (
    <Grid container stackable verticalAlign="middle">
      <Header>
      
      </Header>
    </Grid>
  );
};
export default AllergensAliases;
